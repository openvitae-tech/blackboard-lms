<%= turbo_stream.prepend 'tag-list', partial: 'tags/tag', locals: { tag: @tag } %>
<%= turbo_stream.replace 'create-tag-modal' %>
<%= turbo_stream.update 'tags-count' do %>
  <h1 class="mb-4 text-sm font-semibold"><%= pluralize(@tags_count, "tag") %></h1>
<% end %>
