<%= form_with(model: learning_partner) do |form| %>
  <% if learning_partner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(learning_partner.errors.count, "error") %> prohibited this learning_partner from being saved:</h2>

      <ul>
        <% learning_partner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :about, style: "display: block" %>
    <%= form.text_area :about %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
