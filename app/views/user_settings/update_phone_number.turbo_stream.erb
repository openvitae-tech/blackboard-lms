<% if @user.errors.any? %>
  <%= turbo_stream.replace "modal", partial: "user_settings/edit_phone_number" %>
<% else %>
  <%= turbo_stream.replace "modal", partial: "user_settings/verify_phone_number" %>
<% end %>
