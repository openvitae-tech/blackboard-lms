<% unless @bulk_invite %>
  <%= turbo_stream.prepend "team-member-list", partial: "teams/member_list_items", locals: { team: @team, member: @user } %>
<% end %>
<%= turbo_stream.replace "invite_button_section", partial: "commons/invite_user", locals: { learning_partner: @user.learning_partner, team: @user.learning_partner.parent_team } %>
<%= turbo_stream.replace "payment_plan_details", partial: "commons/payment_plan_details", locals: { learning_partner: @user.learning_partner, payment_plan: @user.learning_partner.payment_plan } %>
<%= turbo_stream.replace "flash", partial: "shared/components/flash" %>
<%= turbo_stream.replace "modal" do %>
  <%= turbo_frame_tag "modal" %>
<% end %>
