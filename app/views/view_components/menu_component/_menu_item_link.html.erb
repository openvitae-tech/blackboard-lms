<%= link_to item.url,
  { class: "px-4 py-2 hover:bg-primary-light-50 text-sm font-medium #{item.extra_classes} whitespace-nowrap" }
    .merge(item.options || {}) do %>
  <%= item.label %>
<% end %>
