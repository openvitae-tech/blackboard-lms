<div  data-vimeo-target="completionOverlay" class="absolute inset-0 flex items-center justify-center z-10 bg-black bg-opacity-80 w-full hidden ">
    <div class="flex flex-col gap-4 items-center">
      <div class="flex flex-col items-center gap-4">
        <%= link_to course_module_lesson_path(course, course_module, lesson),
            data: {
                action: "vimeo#rewatch"
            } do %>
            <%= button(label: "Watch again", icon_name: "arrow-uturn-right", type: "secondary") %>
        <% end %>        
      </div>
      <p class="text-white text-sm mt-2">You’ve reached the end of the video</p>
    </div>
  </div>