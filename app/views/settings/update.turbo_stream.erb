<%= turbo_stream.replace "modal-flash", partial: "shared/components/modal_flash" %>

<%= turbo_stream.replace "settings-page-header-desktop", partial: "settings/page_header_desktop", locals: { user: @user } %>
<%= turbo_stream.replace "settings-page-header-mobile", partial: "settings/page_header_mobile", locals: { user: @user } %>
<%= turbo_stream.replace "settings-page-body-desktop", partial: "settings/page_body_desktop", locals: { user: @user } %>
<%= turbo_stream.replace "settings-page-body-mobile", partial: "settings/page_body_mobile", locals: { user: @user } %>