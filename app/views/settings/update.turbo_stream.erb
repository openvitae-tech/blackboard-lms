<%= turbo_stream.replace "modal-flash", partial: "shared/components/modal_flash" %>

<%= turbo_stream.replace "settings-page-header", partial: "settings/page_header", locals: { user: @user } %>
<%= turbo_stream.replace "settings-page-body", partial: "settings/page_body", locals: { user: @user } %>