<%= render "shared/components/mobile_layout" do %>
  <h1 class="my-4 text-sm font-medium">User settings</h1>
      <h2 class="text-xs text-primary font-semibold">My Profile</h2>

  <%= render "settings/page_header_mobile", user: @user %>
  <%= render "settings/page_body_mobile", user: @user %>
<% end %>