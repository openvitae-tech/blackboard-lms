<%= render "shared/header", header_options(title: "Team management", header_extended: true) %>
<%= render "page_header" %>

<%= render "users/user_list", users: @members %>

<% if current_user.is_admin? %>
  <%= render "users/invite_admin_modal", user: User.new %>
<% else %>
  <%= render "users/invite_modal", user: User.new, learning_partner: @learning_partner %>
<% end %>

<%= turbo_frame_tag "assign-course-frame" %>