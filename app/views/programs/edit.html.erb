<% modal_footer = capture do %>
  <div class="flex gap-2 sticky bottom-0 bg-white w-full">
    <%= button_tag type: "button", class: "flex-1", data: { action: "click->modals#closeModal" } do %>
      <%= button_component(text: "Cancel", type: "outline") %>
    <% end %>
    <%= button_tag form: "edit-program-form", class: "flex-1" do %>
      <%= button_component(text: "Save") %>
    <% end %>
  </div>
<% end %>

<%= modal_box_component(title: t("programs.edit"), modal_footer:) do %>
  <%= form_with(url: program_path(@program), method: :patch, model: @program, scope: :program, id: "edit-program-form", class: "flex flex-col gap-6") do |form| %>
    <div class="flex flex-col gap-4">
      <%= render "shared/components/form_errors", resource: @program %>
      <%= input_text_component(form:, name: :name, label: "Name", placeholder: "Enter the program name") %>
    </div>
  <% end %>
<% end %>
