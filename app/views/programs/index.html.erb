<div class="flex justify-between items-center mb-4">
  <%= heading_component(text: t("programs.label")) %>
  <%= link_to new_program_path, data: { turbo_frame: "modal" }, class: "nav-link" do %>
    <%= button(label: 'Add program', icon_name: "plus") %>
  <% end %>
</div>
<div id="programs-list">
  <h3 class="text-sm mb-4 font-bold"><%= pluralize(@programs.size, 'program') %></h3>
  <div class="mt-6 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
    <% @programs.each do |program| %>
      <%= render "program", program: program %>
    <% end %>
  </div>
</div>
