   <!-- Table Header -->
    <div class="hidden md:grid grid-cols-12 gap-4 text-left text-sm font-medium px-6 py-4 border-b border-line-colour-light w-full">
      <% columns.each do |col| %>
        <div class="<%= col[:span] %> flex <%= col[:alignment] %>"><%= col[:name] %></div>
      <% end %>
    </div>
