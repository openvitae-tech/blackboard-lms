<% if @issue.valid? %>
  <%= turbo_stream.replace "modal" do %>
    <%= turbo_frame_tag "modal" %>
  <% end %>
  <%= turbo_stream.replace "flash", partial: "shared/components/flash" %>
<% else %>
  <%= turbo_stream.replace("modal", partial: "form", locals: { issue: @issue }) %>
<% end %>