<div class="my-6">
  <%= render "shared/components/back_button", back_link: ui_components_path %>
</div>

<%= heading_component(text: 'App components', size: 'lg') %>

<ul class="my-6 flex flex-col gap-2">
  <li>
    <%= heading_component(text: 'Food Production', size: 'md') %>
    <div>
      <%= course_carousal_component(courses: @courses, title: "My courses", count: Course.count, load_path: "") %>
    </div>
  </li>

  <li>
    <%= heading_component(text: 'Housekeeping', size: 'md') %>
    <div>
      <%= course_carousal_component(courses: @courses, title: "My courses", count: Course.count, load_path: "") %>
    </div>
  </li>
</ul>