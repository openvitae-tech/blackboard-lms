<% base_class = 'cursor-pointer text-xs font-normal md:text-sm hover:font-medium' %>
<% color_class = type.to_sym == :danger ? 'text-danger' : 'text-purple' %>
<% final_class = "#{base_class} #{color_class}" %>

<%= link_to text, link, html_options.merge(class: final_class) %>
