
<div class="my-6">
  <%= render "shared/components/back_button", back_link: ui_components_path %>
</div>
<%= h3_component(text: "Profile icon Component") %>

<%= doc_section_component(title: "Small profile icon") do %>
  <%= profile_icon_component(letter: @sample_initial, size: 'sm' ) %>

<% end %>

<%= doc_section_component(title: "Medium profile icon") do %>
  <%= profile_icon_component(letter: @sample_initial, size: 'md' ) %>

<% end %>

<%= doc_section_component(title: "Large profile icon") do %>
  <%= profile_icon_component(letter: @sample_initial, size: 'lg' ) %>

<% end %>
